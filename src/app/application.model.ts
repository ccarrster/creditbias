export class Application {
    amount: number;
    term: string;
    interest: number;
    constructor(amount: number, term: string, interest: number) {
        this.amount = amount;
        this.term = term;
        this.interest = interest;
     }
}